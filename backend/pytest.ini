[pytest]
testpaths = tests
# Restricted to test_*.py on purpose: tests/smoke_test.py is a standalone script (real
# training run, disk-mutating), not a pytest module — it matches the default *_test.py
# pattern too, so without this it would collect and execute alongside the real suite.
python_files = test_*.py
